# Kubernetes service exposing the web GUI
apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  selector:
    app: app
  ports:
  - port: 80             # Service port exposed internally
    targetPort: 8000     # Pod port to forward to
  type: ClusterIP        # Internal-only service
