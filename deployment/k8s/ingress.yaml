apiVersion: networking.k8s.io/v1
kind: Ingress
# Route external traffic to the service
metadata:
  name: app-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app-service
            port:
              number: 80
