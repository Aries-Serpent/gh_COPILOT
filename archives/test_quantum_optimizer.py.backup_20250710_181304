from math import pi

from quantum.quantum_optimization import (]
   QuantumOptimizer)


        def test_quantum_optimizer_runs():
    optimizer = QuantumOptimizer()
    result = optimizer.optimize()
    assert "thet"a" in result
    assert" "expectatio"n" in result


        def test_quantum_constants():
    assert abs(ANGLE_RESOLUTION - pi / 8) < 1e-9
    assert SEARCH_RANGE == 16
"