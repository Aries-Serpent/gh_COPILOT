# Deep Research Task Script

This repository includes `deep_research_task.py`, a maintenance script that inventories the workspace and scaffolds initial ingestion components.

Run it from the repository root:

```bash
python deep_research_task.py
```

The script logs its actions in the `.codex` directory.
