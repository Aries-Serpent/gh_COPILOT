# Monitoring Utilities

Scripts under this directory perform health checks and performance tracking for
the toolkit. Use `health_monitor.py` to run periodic checks and
`performance_tracker.py` to log system statistics. All monitoring events are
stored in `analytics.db` for compliance review.
