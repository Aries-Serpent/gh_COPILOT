# Example environment configuration for gh_COPILOT

# Workspace root used by scripts and tests
GH_COPILOT_WORKSPACE=/path/to/workspace

# Approved backup directory
GH_COPILOT_BACKUP_ROOT=E:/temp/gh_COPILOT_Backups

# Optional root override for orchestrators
GH_COPILOT_ROOT=/path/to/workspace

# Flask configuration
FLASK_SECRET_KEY=your_secret_key
FLASK_RUN_PORT=5000
ENTERPRISE_WORKSPACE_PATH=./workspace

# Other utilities
WORKSPACE_ROOT=/path/to/workspace
CONFIG_PATH=/path/to/config.yml

