# Ingestion Module

This package will host data ingestion features. The current implementation
provides a placeholder :class:`Ingestor` with a single ``ingest`` method that
raises :class:`NotImplementedError`.

Future work will flesh out the ingestion pipeline and corresponding tests.
